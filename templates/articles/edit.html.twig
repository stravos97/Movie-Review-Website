{% extends 'base.html.twig' %}

{% block title %}Edit Articles{% endblock %}



{% block body %}

{#    <form method="POST" action="{{ path('upload_test') }}" enctype="multipart/form-data">#}
{#        <input type="file" name="image">#}

{#        <button type="submit" class="btn btn-primary ">Upload</button>#}
{#    </form>#}


    {{ form_start(form) }} {# This is a post request to the same URL, the same route that the get request comes from #}

{#    {{ form_widget(form) }} #}{# This is used to render a form text box #}
    {{ form_row(form.movie_title) }}
    {{ form_row(form.summary) }}
    {{ form_row(form.message_body) }}
    {{ form_row(form.rating) }}
    {{ form_row(form.director) }}
    {{ form_row(form.actors) }}
    {{ form_row(form.duration) }}
    {{ form_row(form.picture) }}
 
    {{ form_row(form.save) }}

    {{ form_end(form) }}

{% endblock %}