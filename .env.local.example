# Local (non-Docker) developer overrides
# Copy to .env.local and adjust values for your machine.

# Symfony environment
APP_ENV=dev
APP_DEBUG=1

# Database connection for local MySQL
# Example: user `sparta_user` with password `your_password` on localhost
DATABASE_URL="mysql://sparta_user:your_password@127.0.0.1:3306/sparta_academy?serverVersion=8.0"

# Application secret (generate a random string)
APP_SECRET=change_me_local_dev

# Optional: mailer DSN, etc.
# MAILER_DSN=null://localhost

