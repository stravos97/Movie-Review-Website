######################################################################
# Example environment file for local development and Docker Compose.
# Copy to .env and adjust values. Do NOT commit real credentials.
######################################################################

### App ###
APP_ENV=dev
APP_SECRET=change_me

### Database credentials (used by Compose and Symfony) ###
APP_DB_USERNAME=sparta_user
APP_DB_PASSWORD=your_secure_password_here

# Local MySQL root password (for docker-compose.local.yml)
MYSQL_ROOT_PASSWORD=local_root_password_here

# Target database host/port/name
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=sparta_academy

# Full DSN for remote or local DB
DB_URL=mysql://${APP_DB_USERNAME}:${APP_DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?serverVersion=8.0

# Default Doctrine DSN
DATABASE_URL=${DB_URL}

